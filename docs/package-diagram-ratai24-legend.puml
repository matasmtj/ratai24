@startuml ratai24-bce-legend
title Ratai24 — BCE / sluoksnių legenda (MagicDraw)

skinparam shadowing false

note as N1
Boundary (Jacobson B)
React Page, Layout, svarbūs komponentai — formos ir įvykiai.

Controller (server)
Express Routes, Middleware, Controller, PricingService,
kainodaros skaičiavimo klasės.

Entity (Prisma)
Persistence.Entities — lentelių modeliai ir enumeracijos.

Gateway / Adapter
ApiClient, ApiGateways (Axios), Cloudinary, Resend, reCAPTCHA.

HTTP riba
Tarp Ratai24.Frontend ir Ratai24.Backend (naršyklė su REST JSON).

Frontend ApiGateways
«Gateway» arba lengvas «Control» kliento pusėje — aiškiai paaiškinti įvade.
end note

@enduml
